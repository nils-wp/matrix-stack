# =============================================================================
# MATRIX STACK - ENVIRONMENT VARIABLES
# Kopiere diese Datei nach .env und fülle alle Werte aus
# =============================================================================

# -----------------------------------------------------------------------------
# DOMAIN KONFIGURATION
# -----------------------------------------------------------------------------
# Basis-Domain für Matrix User IDs (@user:wolfbraun.link)
BASE_DOMAIN=wolfbraun.link

# Escaped Version für Regex (Punkte escapen)
BASE_DOMAIN_ESCAPED=wolfbraun\\.link

# Synapse API Domain
SYNAPSE_DOMAIN=matrix.wolfbraun.link

# Element Web Client Domain
ELEMENT_DOMAIN=element.wolfbraun.link

# -----------------------------------------------------------------------------
# DATABASE
# -----------------------------------------------------------------------------
# PostgreSQL Passwort (ÄNDERN! Mindestens 32 Zeichen)
DB_PASSWORD=CHANGE_ME_GENERATE_SECURE_PASSWORD_HERE

# -----------------------------------------------------------------------------
# SYNAPSE SECRETS
# Generieren mit: openssl rand -hex 32
# -----------------------------------------------------------------------------
REGISTRATION_SECRET=CHANGE_ME_openssl_rand_hex_32
MACAROON_SECRET=CHANGE_ME_openssl_rand_hex_32
FORM_SECRET=CHANGE_ME_openssl_rand_hex_32

# -----------------------------------------------------------------------------
# WHATSAPP BRIDGE TOKENS
# Generieren mit: openssl rand -hex 32
# -----------------------------------------------------------------------------
WHATSAPP_AS_TOKEN=CHANGE_ME_openssl_rand_hex_32
WHATSAPP_HS_TOKEN=CHANGE_ME_openssl_rand_hex_32

# -----------------------------------------------------------------------------
# TELEGRAM BRIDGE
# API Credentials von https://my.telegram.org (unter "API development tools")
# Bot Token von @BotFather
# -----------------------------------------------------------------------------
TELEGRAM_API_ID=PLACEHOLDER_GET_FROM_MY_TELEGRAM_ORG
TELEGRAM_API_HASH=PLACEHOLDER_GET_FROM_MY_TELEGRAM_ORG
TELEGRAM_BOT_TOKEN=PLACEHOLDER_GET_FROM_BOTFATHER

# Bridge Tokens (Generieren mit: openssl rand -hex 32)
TELEGRAM_AS_TOKEN=CHANGE_ME_openssl_rand_hex_32
TELEGRAM_HS_TOKEN=CHANGE_ME_openssl_rand_hex_32

# -----------------------------------------------------------------------------
# SLACK / MATTERBRIDGE
# Slack App erstellen: https://api.slack.com/apps
# Bot Token beginnt mit xoxb-
# -----------------------------------------------------------------------------
SLACK_BOT_TOKEN=PLACEHOLDER_GET_FROM_SLACK_API

# Matrix User für Matterbridge (wird manuell angelegt)
MATTERBRIDGE_PASSWORD=CHANGE_ME_SECURE_PASSWORD

# =============================================================================
# NACH DEM AUSFÜLLEN:
# 1. Alle CHANGE_ME Werte ersetzen
# 2. Secrets generieren mit: openssl rand -hex 32
# 3. Telegram API Credentials holen
# 4. Slack App erstellen und Bot Token kopieren
# =============================================================================
